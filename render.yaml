services:
  - type: web
    name: cinema-management
    env: docker
    dockerfilePath: Dockerfile
    
    envVars:
      # PHẢI DÙNG 'value:' cho tất cả các biến DB
      - key: DB_HOST
        value: sql104.infinityfree.com
      - key: DB_USER
        value: if0_40669531
      - key: DB_PASSWORD
        value: zxhnv4zxYg
      - key: DB_NAME
        value: if0_40669531_cinema_management
      - key: DB_PORT
        value: 3306
      
      # Các biến tối ưu khác
      - key: ENVIRONMENT
        value: production
      - key: ALLOWED_ORIGIN
        value: ${EXTERNAL_URL}
      - key: VITE_API_URL 
        value: ${EXTERNAL_URL}/api